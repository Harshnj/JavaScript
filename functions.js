//functions are resulable code
//We can resue these function by calling them by their name and paranthesis

function add(x,y){
    return x+y;
}

function sub(x,y){
    return x-y;
}

let num1 = add(2,3);
let num2= sub(3,2);

console.log(`The result of addition is ${num1} and substraction is ${num2}`)

