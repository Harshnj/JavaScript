// /an arrow function is a concise way to write function expressions. Arrow functions were introduced in ECMAScript 6 (ES6) and provide a more compact syntax compared to traditional function expressions.

const result= (a,b) =>{                //Result is a function variables
    console.log(a+b);
};

result(2,3);
